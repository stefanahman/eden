# Example Eden Branch Rule

This rule was contributed by the Eden example branch. Claude Code reads rule
files under `~/.claude/rules/` for project-specific conventions.

graft-claude symlinks branch-supplied rules, agents, and skills into
`~/.claude/{rules,agents,skills}/` so multiple branches can contribute
without conflicts.

Replace this file with your real rules in your private branches.
