{
  "$comment": "Model Context Protocol (MCP) Configuration - Default Branch",
  "$comment2": "Eden's opinionated MCP server defaults",
  "$comment3": "Add more servers in your personal branches: ~/branch-work/.config/mcp/servers.json",
  "mcpServers": {
    "github-eden": {
      "command": "sh",
      "args": [
        "-c",
        "PATH=\"$HOME/.eden/bin:$HOME/.volta/bin:/usr/local/bin:/usr/bin:/bin\" OP_BIOMETRIC_UNLOCK_ENABLED=true GITHUB_PERSONAL_ACCESS_TOKEN=$(op read 'op://Private/eden-github-mcp-token/credential' 2>/dev/null || echo '') pnpm dlx @modelcontextprotocol/server-github"
      ]
    }
  }
}

