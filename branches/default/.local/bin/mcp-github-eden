#!/bin/bash
# MCP wrapper: GitHub server for Eden
# Fetches token from 1Password at runtime — never stored on disk.
set -euo pipefail

export PATH="$HOME/.eden/bin:$HOME/.volta/bin:$HOME/.local/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin"
export OP_BIOMETRIC_UNLOCK_ENABLED=true
GITHUB_PERSONAL_ACCESS_TOKEN=$(op read 'op://Developer/eden-github-mcp-token/credential' --account my.1password.com)
export GITHUB_PERSONAL_ACCESS_TOKEN

exec pnpm dlx @modelcontextprotocol/server-github
