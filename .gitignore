# Local notes and ideas (not for public repo)
.ideas
.notes

# Secret patterns (never commit these)
*.key
*.secret
*.token
.env
.env.*

# Claude Code local settings (machine-specific, auto-managed)
.claude/settings.local.json

# OS artifacts
.DS_Store

# Log files
*.log
