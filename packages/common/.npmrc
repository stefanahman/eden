# npm configuration (fallback when pnpm isn't used)
# XDG-compliant directories
# Note: npm only expands ${HOME} — other env vars like ${XDG_CACHE_HOME} are treated as literals.
prefix=${HOME}/.local
cache=${HOME}/.cache/npm
init-module=${HOME}/.config/npm/config/npm-init.js

